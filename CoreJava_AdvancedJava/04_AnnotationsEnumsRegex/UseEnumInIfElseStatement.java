public enum Directions {
    EAST, 
    WEST, 
    NORTH, 
    SOUTH
}
// here the Directions is a variable of Enum Type, which is a collection of four constants EAST, WEST, NORTH, SOUTH

Directions dir = Directions.NORTH;

if(dir == Directions.EAST) {
    // do something, write your logic
} else if(dir == Directions.WEST) {
    // do something else
} else {
    // do domething, write logic for the remaining
}

